# Adjacency Set JS implementation
## Undirected graph
The graph is a Map.  
The key of each key-value pair is a node.  
The value is a Set of edges (destination nodes).  
## Directed graph
The graph is an Object